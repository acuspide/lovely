/**
 * URL base del backend. En un proyecto más grande esto viviría en los
 * environments de Angular (dev/prod); aquí basta una constante única porque
 * F-01 solo se ejecuta contra el backend local durante el desarrollo.
 */
export const API_BASE_URL = 'http://localhost:8080/api';
